<h1>Create Product</h1>
<%= form_for(@product) do |f|%>
<table>
    <tr>
      <th>Title:</th>
      <td><%= f.text_field(:title, size:30)%> </td>
    </tr>

    <tr>
      <th>Description</th>
      <td><%=f.text_area(:description, size: "40x20") %></td>
    </tr>

    <tr>
      <th>Image url:</th>
      <td><%= f.text_field(:image_url, size:30 )%></td>
    </tr>>
    <tr>
      <th>Price:</th>
      <td><%= f.text_field(:price ) %></td>
    </tr>
</table>
    <%=f.submit%>
    <%= link_to 'Back', root_path%>

<%end%>
